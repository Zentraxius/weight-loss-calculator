import $ from 'jquery';
import 'bootstrap';
import 'bootstrap/dist/css/bootstrap.min.css';
import './styles.css';

$(document).ready(function () {
  // $('#submit-button').submit(function (event) {
  //     event.preventDefault();


  // to call your API key you must first add it to your .env file and then use `${process.env.API_KEY}` to call it.
  // });
});